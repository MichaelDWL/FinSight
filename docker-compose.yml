services:
  backend:
    build:
      context: ./backend
    container_name: finsight-backend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: 3000
      CORS_ORIGIN: "*"
    restart: unless-stopped
