{
  "name": "finsight-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Compat: scripts locais apontam para src/ na raiz do monorepo.",
  "main": "./src/server.js",
  "scripts": {
    "start": "npm start --prefix ..",
    "stop": "npm run stop --prefix ..",
    "restart": "npm run restart --prefix ..",
    "dev": "npm run dev --prefix ..",
    "start:supabase": "npm run start:supabase --prefix ..",
    "migrate": "npm run migrate --prefix ..",
    "migrate:supabase": "npm run migrate:supabase --prefix ..",
    "cron:market": "npm run cron:market --prefix ..",
    "test": "npm test --prefix ..",
    "lint": "npm run lint --prefix .."
  }
}
